DATABASE_URL=postgresql+psycopg://radica_user:radica_pass@localhost:5432/radica_mun
JWT_SECRET=cambia-esta-clave-en-produccion
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480

STORAGE_PATH=../storage
ALLOWED_ORIGINS=http://localhost:4200

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=radicacion@municipio.gov.co

# Clave Fernet para cifrar las app passwords del buzón oficial.
# Generar con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=
